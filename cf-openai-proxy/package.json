{
  "name": "topgre-openai-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Token-gated Cloudflare Worker that proxies OpenAI so the API key stays server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
